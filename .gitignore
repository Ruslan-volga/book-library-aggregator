# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage
coverage/
.nyc_output

# Logs
logs
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
.dockerignore

# Uploads (если храните локально)
backend/uploads/*
!backend/uploads/.gitkeep

# Build
backend/dist/
frontend/build/
Настройте бэкенд

bash
cd backend
npm install
npm start
# Сервер запустится на http://localhost:3001
Настройте фронтенд

bash
cd frontend
npm install
npm start
# Приложение откроется на http://localhost:3000
🏗️ Архитектура проекта
text
book-library-aggregator/
├── frontend/                 # React + TypeScript + Material-UI
│   ├── src/
│   │   ├── components/      # UI компоненты
│   │   │   ├── Navigation.tsx
│   │   │   ├── BookSearch.tsx
│   │   │   └── ...
│   │   ├── pages/          # Страницы
│   │   │   ├── HomePage.tsx
│   │   │   ├── BooksPage.tsx
│   │   │   └── LibrariesPage.tsx
│   │   ├── api/            # API клиент
│   │   │   └── client.ts
│   │   └── App.tsx         # Главный компонент
│   ├── package.json
│   └── tsconfig.json
├── backend/                 # Express.js сервер
│   ├── server.js           # Основной сервер
│   ├── auth.js             # Логика аутентификации
│   ├── package.json
│   └── README.md
└── README.md               # Этот файл
🔧 Технологический стек
Фронтенд
React 18 с хуками

TypeScript для типизации

Material-UI 5 компоненты

React Router 6 навигация

Axios HTTP запросы

Context API управление состоянием

Бэкенд
Express.js веб-фреймворк

CORS кросс-доменные запросы

JWT аутентификация

REST API архитектура

📡 API Endpoints
🔐 Аутентификация
Метод	Endpoint	Описание
POST	/api/auth/login	Вход в систему
POST	/api/auth/register	Регистрация
GET	/api/auth/me	Данные пользователя
📚 Библиотеки
Метод	Endpoint	Описание
GET	/api/libraries	Все библиотеки
GET	/api/libraries/stats/counts	Статистика
📖 Книги
Метод	Endpoint	Описание
GET	/api/books	Все книги
GET	/api/libraries/books/search	Поиск книг
POST	/api/libraries/books/new	Добавить книгу
🩺 Системные
Метод	Endpoint	Описание
GET	/api/health	Проверка здоровья
GET	/api/protected	Тест защищенного маршрута
GET	/api	Информация о API
🎨 Интерфейс пользователя
🏠 Главная страница
Навигационная панель — меню и аутентификация

Статистика — количество библиотек и книг

Поиск книг — с мгновенной фильтрацией

Список библиотек — с детальной информацией

📚 Страница книг
Карточки книг — с полной информацией

Фильтрация — по названию, автору

Индикация доступности — количество экземпляров

🏛️ Страница библиотек
Список библиотек — с адресами и описаниями

Количество книг — в каждой библиотеке

Детальная информация — контакты и описание

🔐 Аутентификация
Система использует JWT токены:

javascript
// Пример запроса на вход
POST /api/auth/login
{
  "email": "user@example.com",
  "password": "password123"
}

// Ответ
{
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "user": {
    "id": 1,
    "email": "user@example.com",
    "name": "User Name"
  }
}
🧪 Тестовые данные
👥 Пользователи
Администратор: admin@library.com / admin123

Пользователь: user@library.com / user123

🏛️ Библиотеки
Центральная библиотека им. Пушкина

Адрес: ул. Пушкина, 1

Описание: Главная библиотека города

Книг: 2

Городская библиотека

Адрес: ул. Ленина, 10

Описание: Культурно-образовательный центр

Книг: 2

📖 Книги
Война и мир — Лев Толстой (1869)

Преступление и наказание — Фёдор Достоевский (1866)

Хоббит — Джон Р. Р. Толкин (1937)

🚀 Разработка
🔄 Запуск в режиме разработки
bash
# Запуск бэкенда (терминал 1)
cd backend
npm run dev

# Запуск фронтенда (терминал 2)
cd frontend
npm start
📦 Сборка для production
bash
# Сборка фронтенда
cd frontend
npm run build

# Запуск бэкенда
cd backend
npm start
📁 Файлы конфигурации
Frontend .env
env
REACT_APP_API_URL=http://localhost:3001
REACT_APP_NAME=Book Library Aggregator
Backend настройки CORS
javascript
app.use(cors({
  origin: ['http://localhost:3000', 'http://127.0.0.1:3000'],
  credentials: true,
  methods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'],
  allowedHeaders: ['Content-Type', 'Authorization']
}));
🐛 Решение проблем
❗ Проблема: "Cannot GET /api/books"
Решение: Проверьте, запущен ли бэкенд на порту 3001

❗ Проблема: CORS ошибки
Решение: Обновите настройки CORS в backend/server.js

❗ Проблема: "Proxy error"
Решение: Проверьте proxy в frontend/package.json

❗ Проблема: Книги не загружаются
Решение:

Откройте консоль браузера (F12 → Console)

Проверьте Network вкладку

Убедитесь, что endpoint /api/books возвращает данные

Проверьте URL в client.ts

🎯 Примеры кода
🔍 Компонент поиска книг
tsx
// BookSearch.tsx
const handleSearch = async (query: string) => {
  const response = await fetch(`${API_URL}/api/books`);
  const allBooks = await response.json();
  
  return allBooks.filter(book =>
    book.title.toLowerCase().includes(query.toLowerCase()) ||
    book.author.toLowerCase().includes(query.toLowerCase())
  );
};
📊 Загрузка статистики
tsx
// App.tsx
useEffect(() => {
  fetch('http://localhost:3001/api/libraries/stats/counts')
    .then(res => res.json())
    .then(data => setStats(data));
}, []);
📈 Планы развития
🚧 В разработке
Пагинация для списка книг

Система рецензий и оценок

Электронное бронирование книг

📅 В планах
Интеграция с базой данных

Уведомления по email

Мобильное приложение

Экспорт данных

API документация

🤝 Участие в разработке
Форкните репозиторий

Создайте ветку (git checkout -b feature/AmazingFeature)

Закоммитьте изменения (git commit -m 'Add AmazingFeature')

Запушьте ветку (git push origin feature/AmazingFeature)

Откройте Pull Request

📄 Лицензия
Этот проект лицензирован под MIT License - смотрите файл LICENSE для деталей.

👤 Автор
Ruslan Volga

GitHub: @Ruslan-volga

Проект: Book Library Aggregator

🙏 Благодарности
Create React App за отличный шаблон

Material-UI за прекрасные компоненты

Express.js за простой и мощный бэкенд

Сообществу разработчиков за вдохновение

📞 Поддержка
Нашли баг или есть предложение?
Создайте issue или свяжитесь с автором.

⭐ Если проект вам понравился, поставьте звезду на GitHub! ⭐

🎉 Успешного использования Book Library Aggregator!
text

## Git команды для сохранения и пуша:

```bash
# 1. Сохраните README.md в корень проекта
# Поместите файл в /путь/к/book-library-aggregator/README.md

# 2. Проверьте текущий статус
git status

# 3. Добавьте файл
git add README.md

# 4. Сделайте коммит
git commit -m "Добавлен полный README.md с документацией проекта"

# 5. Запушьте в репозиторий
git push origin main

# Если нужно обновить существующий README:
git add .
git commit -m "Обновление документации: добавлены разделы, инструкции, API документация"
git push origin main
Дополнительные файлы которые можно добавить:
1. .gitignore (если еще нет)
gitignore
# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build
dist/
build/
out/
.next/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
2. package.json для корня (опционально)
json
{
  "name": "book-library-aggregator",
  "version": "1.0.0",
  "description": "Full-stack book library management system with React frontend and Express backend",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:frontend": "cd frontend && npm start",
    "start:backend": "cd backend && npm start",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm start",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd frontend && npm run build",
    "test": "echo \"No tests specified\""
  },
  "keywords": [
    "library",
    "books",
    "react",
    "express",
    "typescript",
    "material-ui",
    "nodejs"
  ],
  "author": "Ruslan Volga",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ruslan-volga/book-library-aggregator.git"
  },
  "bugs": {
    "url": "https://github.com/Ruslan-volga/book-library-aggregator/issues"
  },
  "homepage": "https://github.com/Ruslan-volga/book-library-aggregator#readme",
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}